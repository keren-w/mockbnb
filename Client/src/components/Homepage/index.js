import React from 'react';
import LocationsIndex from '../LocationsIndex';

// This tiny component exists so we can modify which components we want in the homepage anytime.
export default () => <LocationsIndex />